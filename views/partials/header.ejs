<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'AI Recipe Generator' %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .nav {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 16px;
            padding: 20px 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }
        
        .logo {
            font-size: 24px;
            font-weight: 700;
            color: #667eea;
            text-decoration: none;
        }
        
        .nav-links {
            display: flex;
            gap: 25px;
            align-items: center;
        }
        
        .nav-link {
            color: #555;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s;
        }
        
        .nav-link:hover {
            color: #667eea;
        }
        
        .nav-link.active {
            color: #667eea;
            font-weight: 600;
        }
        
        .content {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            min-height: 400px;
        }
    </style>
</head>
<body>
    <div class="container">
        <nav class="nav">
            <a href="/" class="logo">🍳 Chef AI</a>
            <div class="nav-links">
                <a href="/" class="nav-link <%= currentPage === 'home' ? 'active' : '' %>">Trang chủ</a>
                <a href="/demo" class="nav-link <%= currentPage === 'demo' ? 'active' : '' %>">Demo</a>
                <a href="/suggest" class="nav-link <%= currentPage === 'suggest' ? 'active' : '' %>">Gợi ý</a>
                <a href="/chat-ui" class="nav-link <%= currentPage === 'chat' ? 'active' : '' %>">Chat</a>
                <a href="/search-ui" class="nav-link <%= currentPage === 'search' ? 'active' : '' %>">Tìm kiếm</a>
                <a href="/status" class="nav-link <%= currentPage === 'status' ? 'active' : '' %>">Trạng thái</a>
            </div>
        </nav>
        <div class="content">

